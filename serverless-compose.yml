services:
  backend-auth:
    path: auth

  backend-api:
    path: api
    dependsOn: 
      - backend-auth
      - backend-data

  backend-auth-public:
    path: auth-public
    dependsOn: 
      - backend-api
      - backend-auth

  backend-data:
    path: data
